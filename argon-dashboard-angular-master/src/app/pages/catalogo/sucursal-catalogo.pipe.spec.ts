import { SucursalCatalogoPipe } from './sucursal-catalogo.pipe';

describe('SucursalCatalogoPipe', () => {
  it('create an instance', () => {
    const pipe = new SucursalCatalogoPipe();
    expect(pipe).toBeTruthy();
  });
});
