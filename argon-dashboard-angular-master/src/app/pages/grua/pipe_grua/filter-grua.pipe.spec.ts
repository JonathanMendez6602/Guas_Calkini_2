import { FilterGruaPipe } from './filter-grua.pipe';

describe('FilterGruaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGruaPipe();
    expect(pipe).toBeTruthy();
  });
});
