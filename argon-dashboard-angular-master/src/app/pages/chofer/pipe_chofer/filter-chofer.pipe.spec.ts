import { FilterChoferPipe } from './filter-chofer.pipe';

describe('FilterChoferPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChoferPipe();
    expect(pipe).toBeTruthy();
  });
});
