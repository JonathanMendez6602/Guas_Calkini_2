import { FilterAseguradoraPipe } from './filter-aseguradora.pipe';

describe('FilterAseguradoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAseguradoraPipe();
    expect(pipe).toBeTruthy();
  });
});
