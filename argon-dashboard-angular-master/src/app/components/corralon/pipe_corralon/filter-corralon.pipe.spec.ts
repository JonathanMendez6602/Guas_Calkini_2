import { FilterCorralonPipe } from './filter-corralon.pipe';

describe('FilterCorralonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCorralonPipe();
    expect(pipe).toBeTruthy();
  });
});
