import { FilterServicioPipe } from './filter-servicio.pipe';

describe('FilterServicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServicioPipe();
    expect(pipe).toBeTruthy();
  });
});
